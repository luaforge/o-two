* Bugs:

    -- Parsing de IDL: O parsing pega codigo comentado, por exemplo:

      struct foo{
        //string a;
        string b;
      }

      O parser cria o campo 'a' na struct, apesar dele estar comentado.

    -- O parser nao consegue carregar a IDL do servico de nomes

    -- Bug misterioso de comunicação: as vezes, a ORB sinaliza um erro
       de comunicacao quando aparentemente a comunicacao foi bem sucedida.


  * Whishlist:

     -- Suporte a comentarios de bloco no parser(/* .... */)

     -- Suporte a includes nas IDLs

     -- Suporte a mais tipos IDL, pelo menos o necessario para fazer o parsing
        da idl do Serviço de Nomes (CosNaming.idl)

     -- Documentacao basica, principalmente nos seguintes assuntos:

        -- Bind IDL -> O2 (Nao eh identico ao LuaORB)
        -- Funcionalidades suportadas ( Tipos IDL, etc...)
        -- Como funciona o mapeamento de excecoes para Lua, que tipo de excecao o O2
           suporta,etc....

     -- Suporte a declaracao e uso de um tipo em um MESMO modulo. Hoje temos que
        fazer assim:

        module submodule{

          typedef sequence<octet> FileContent;

        }

        module mainModule{

          struct File{
            string Filename;
            FileContent contents;
          }

        }

     -- Suporte a metodos Oneway

     -- Suporte a modulos aninhados

     -- Processamento concorrente Cliente/Servidor na ORB(O Pedro disse que estava fazendo

        isso)

     -- Melhores indicacoes de onde esta o erro, no caso de uma IDL com erros.
